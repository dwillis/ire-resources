 Unfortunately I do not have enough context to generate the full JSON object you described based on an input text. If you provide the input text you would like me to parse into the JSON format, I would be happy to attempt creating the JSON object with the specified keys and values that you outlined. Please provide the input text and I will do my best to parse it into the requested JSON format. Without any input text to work with, I do not have enough information to populate the JSON keys and values.
